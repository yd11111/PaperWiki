paper: OmniVoice
reviewer: auto
date: 2026-06-03
conclusion: pass-with-fixes
total_issues: 3
high: 0
medium: 1
low: 2

principles:
  可复述:
    verdict: pass
    notes: >
      方法节对两个核心创新 (full-codebook masking, LLM init) 都包含 WHY 解释 +
      消融验证 + 与已有方案 (per-layer masking, random init) 的对比。
      速查卡片"可借鉴"列出 4 个具体可迁移 trick。
  可信赖:
    verdict: pass
    notes: >
      数字 claim 标注覆盖率 > 90%。指标名使用正确 (WER/CER/SIM-o/UTMOS/CMOS/SMOS/RTF)。
      速查卡片指标字段含具体数字 + 数据集名 + 表格出处。
      对 FLEURS CER < ground truth 的结果有审慎解读 [agent 解读]。
  可区分:
    verdict: pass
    notes: >
      方法节全程使用 [论文原文] / [agent 解读] 标注,覆盖率 > 80%。
      agent 解读的推断 (如 causal→bidirectional 迁移的可能原因、FLEURS CER 的解释)
      都有明确的推断标记,不与论文原文 claim 混淆。
  可定位:
    verdict: pass-with-fixes
    notes: >
      KB 背景有具体谱系定位 (MaskGCT 对比 + LLM-based TTS 对比 + SOTA 数字对比)。
      frontmatter models 字段仅列 SoundStorm 和 CosyVoice 3,缺少 MaskGCT/F5-TTS/ZipVoice
      等直接对比的论文笔记链接 (存在于 论文笔记/ 但不在 模型库/)。属于 medium issue。
  不污染:
    verdict: pass
    notes: >
      无新概念页创建需求。反向更新内容为追加 (key_papers 列表 + 末尾一行说明),
      不涉及实质性修改,不会改变已有实体页 status。

issues:
  - id: 1
    type: template-compliance
    severity: medium
    principle: 可定位
    location: frontmatter.models
    detail: >
      models 字段仅列 SoundStorm 和 CosyVoice 3 (模型库实体页),但本文直接对比的
      baseline 还包括 MaskGCT、F5-TTS、ZipVoice、Qwen3-TTS、VoxCPM、IndexTTS2,
      这些在 论文笔记/ 有对应笔记但无 模型库/ 实体页。建议: 至少在 models 字段用
      论文笔记链接补充核心对比 baseline (MaskGCT, F5-TTS, ZipVoice)。
    suggestion: >
      models 字段追加: "[[论文笔记/MaskGCT|MaskGCT]]", "[[论文笔记/Qwen3-TTS|Qwen3-TTS]]"
  - id: 2
    type: traceability-gap
    severity: low
    principle: 可信赖
    location: 训练策略 > 模型参数
    detail: >
      Note 提到 "Qwen3-0.6B backbone" 但未明确提及 Table 1 中报告的总参数量 0.8B
      (含 tokenizer 和 prediction heads)。读者可能困惑 0.6B 是否是完整参数量。
    suggestion: 在训练策略节补充 "总参数量 0.8B (含 audio tokenizer + prediction heads)"
  - id: 3
    type: weak-reusability
    severity: low
    principle: 可复述
    location: 可复用的 idea > #2
    detail: >
      "AR LLM → NAR 初始化" 描述为"零成本",但实际上需要 backbone 架构与 LLM 结构
      完全相同 (OmniVoice 的 backbone 就是 Qwen3-0.6B 架构),对不同架构的 NAR 模型
      不一定适用。
    suggestion: 补充 "前提是 NAR backbone 与 LLM 架构相同"
